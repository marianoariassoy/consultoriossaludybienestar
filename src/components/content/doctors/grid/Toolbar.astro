<div
  class="sticky top-24 md:top-20 w-full flex bg-white dark:bg-muted-800 border border-muted-200 dark:border-muted-700 rounded divide-x divide-muted-200 dark:divide-muted-700 shadow-xl shadow-muted-300/30 dark:shadow-muted-900/30 overflow-x-auto z-30"
>
  <button
    class="group flex-1 flex items-center px-5 py-3 whitespace-nowrap font-sans text-muted-800 dark:text-muted-100 hover:bg-muted-50 dark:hover:bg-muted-700 transition-colors duration-300"
    :class="activeCategory === 'all' ? 'bg-muted-50 dark:bg-muted-700' : ''"
    @click="activeCategory = 'all'"
  >
    <span
      class="flex items-center justify-center h-10 w-10"
      :class="activeCategory === 'all' ? 'text-rose-500' : 'text-muted-400 group-hover:text-primary-500'"
    >
      <iconify-icon class="iconify w-6 h-6 flex items-center justify-center text-2xl" icon="ph:first-aid-duotone"></iconify-icon>
    </span>
    <span>Todos</span>
  </button>

  <button
    class="group flex-1 flex items-center px-5 py-3 whitespace-nowrap font-sans text-muted-800 dark:text-muted-100 hover:bg-muted-50 dark:hover:bg-muted-700 transition-colors duration-300"
    :class="activeCategory === 'psicologia' ? 'bg-muted-50 dark:bg-muted-700' : ''"
    @click="activeCategory = 'psicologia'"
  >
    <span
      class="flex items-center justify-center h-10 w-10"
      :class="activeCategory === 'psicologia' ? 'text-rose-500' : ' text-muted-400 group-hover:text-primary-500'"
    >
      <iconify-icon class="iconify w-6 h-6 flex items-center justify-center text-2xl" icon="ph:heartbeat-duotone"></iconify-icon>
    </span>
    <span>Psicología</span>
  </button>
  <button
    class="group flex-1 flex items-center px-5 py-3 whitespace-nowrap font-sans text-muted-800 dark:text-muted-100 hover:bg-muted-50 dark:hover:bg-muted-700 transition-colors duration-300"
    :class="activeCategory === 'psiquiatria' ? 'bg-muted-50 dark:bg-muted-700' : ''"
    @click="activeCategory = 'psiquiatria'"
  >
    <span
      class="flex items-center justify-center h-10 w-10"
      :class="activeCategory === 'psiquiatria' ? 'text-rose-500' : 'text-muted-400 group-hover:text-primary-500'"
    >
      <iconify-icon class="iconify w-6 h-6 flex items-center justify-center text-2xl" icon="ph:brain-duotone"></iconify-icon>
    </span>
    <span>Psiquiatría</span>
  </button>
  <button
    class="group flex-1 flex items-center px-5 py-3 whitespace-nowrap font-sans text-muted-800 dark:text-muted-100 hover:bg-muted-50 dark:hover:bg-muted-700 transition-colors duration-300"
    :class="activeCategory === 'psicopedagogia' ? 'bg-muted-50 dark:bg-muted-700' : ''"
    @click="activeCategory = 'psicopedagogia'"
  >
    <span
      class="flex items-center justify-center h-10 w-10"
      :class="activeCategory === 'psicopedagogia' ? 'text-rose-500' : 'text-muted-400 group-hover:text-primary-500'"
    >
      <iconify-icon class="iconify w-6 h-6 flex items-center justify-center text-2xl" icon="ph:baby-duotone"></iconify-icon>
    </span>
    <span>Psicopedagogía</span>
  </button>
 
  <button
    class="group flex-1 flex items-center px-5 py-3 whitespace-nowrap font-sans text-muted-800 dark:text-muted-100 hover:bg-muted-50 dark:hover:bg-muted-700 transition-colors duration-300"
    :class="activeCategory === 'nutricion' ? 'bg-muted-50 dark:bg-muted-700' : ''"
    @click="activeCategory = 'nutricion'"
  >
    <span
      class="flex items-center justify-center h-10 w-10"
      :class="activeCategory === 'nutricion' ? 'text-rose-500' : 'text-muted-400 group-hover:text-primary-500'"
    >
      <iconify-icon class="iconify w-6 h-6 flex items-center justify-center text-2xl" icon="ph:person-duotone"></iconify-icon>
    </span>
    <span>Nutrición</span>
  </button>
  <button
    class="group flex-1 flex items-center px-5 py-3 whitespace-nowrap font-sans text-muted-800 dark:text-muted-100 hover:bg-muted-50 dark:hover:bg-muted-700 transition-colors duration-300"
    :class="activeCategory === 'masoterapia' ? 'bg-muted-50 dark:bg-muted-700' : ''"
    @click="activeCategory = 'masoterapia'"
  >
    <span
      class="flex items-center justify-center h-10 w-10"
      :class="activeCategory === 'masoterapia' ? 'text-rose-500' : 'text-muted-400 group-hover:text-primary-500'"
    >
      <iconify-icon class="iconify w-6 h-6 flex items-center justify-center text-2xl" icon="ph:hand-soap-duotone"></iconify-icon>
    </span>
    <span>Masoterapia</span>
  </button>

  <button
  class="group flex-1 flex items-center px-5 py-3 whitespace-nowrap font-sans text-muted-800 dark:text-muted-100 hover:bg-muted-50 dark:hover:bg-muted-700 transition-colors duration-300"
  :class="activeCategory === 'talleres' ? 'bg-muted-50 dark:bg-muted-700' : ''"
  @click="activeCategory = 'talleres'"
>
  <span
    class="flex items-center justify-center h-10 w-10"
    :class="activeCategory === 'talleres' ? 'text-rose-500' : 'text-muted-400 group-hover:text-primary-500'"
  >
    <iconify-icon class="iconify w-6 h-6 flex items-center justify-center text-2xl" icon="ph:calendar-duotone"></iconify-icon>
  </span>
  <span>Talleres</span>
</button>
</div>
